site_name: Meeting Minutes & Agendas
docs_dir: meetings

theme:
  name: material
  font: false # Prevents embedding external font styles
  features:
    - navigation.prune # CRITICAL: Strips full navigation tree from individual page HTML
    - search.suggest
    - search.highlight
