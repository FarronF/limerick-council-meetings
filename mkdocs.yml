site_name: Meeting Minutes & Agendas
docs_dir: docs

# theme:
#   name: mkdocs
#   custom_dir: overrides
theme:
  name: material
  custom_dir: overrides
  features:
    - navigation.path # Enables clickable breadcrumb trails above titles
    - search.suggest
    - search.highlight

nav:
  - Home: index.md
  - Browse Archive: meetings
  - Search Portal: search.md
